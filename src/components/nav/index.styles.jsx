import styled from "styled-components";

export const NavContainer = styled.nav`
display: flex;
align-items: center;
gap: 10px;
padding: 20px;

img{
    width: 50px;
    height: 50px;
    margin: 0 10px;
}

span{
  color: var(--green);
}

`;